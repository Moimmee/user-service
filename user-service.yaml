apiVersion: v1
kind: Service

metadata:
  name: user-service

spec:
  type: NodePort
  selector:
    app: user-app
  ports:
    - port: 9091
      targetPort: 8080
      protocol: TCP
      nodePort: 30001